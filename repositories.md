# Repositories

<!--
This file is user-owned input. Edit it by hand to control what RepoWiki
indexes. The agent never edits this file after init.

Each entry starts with `- uri:`; indented `key: value` lines beneath it
belong to that entry. Only `uri` is required.

  - uri: git@github.com:company/api.git     # any URI `git clone` accepts
    alias: company-api                      # optional display name / slug
    ref: main                               # optional branch, tag, or commit
    tags: backend, typescript, production   # optional comma-separated tags

Never put tokens, passwords, or credential-embedding URLs here. Private
repositories authenticate through your existing local git setup (SSH agent,
credential helpers, gh, glab).
-->

- uri: https://github.com/pavelsimo/pxgo.git
  alias: pxgo

- uri: https://github.com/pavelsimo/shapes.git
  alias: shapes

- uri: https://github.com/pavelsimo/ani.git
  alias: ani

- uri: https://github.com/pavelsimo/fascat.git
  alias: fascat
